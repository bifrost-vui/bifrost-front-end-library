<div class="bg-primary p-3">
    <div class="d-flex align-items-center justify-content-center w-100 vui-anim--fade-slide-in">
        {% include '@atoms/06-icons/icon-svg.twig' with {
            iconName: iconName|default('checkmark'),
            iconSize: iconSize|default('3'),
            iconClass:"px-1 mb-0"
        } only %}
        <p class="mb-0 ml-2"><strong>{{ itemName|default('Cloud DVR unlimited') }}</strong> {{ itemLabelAdded|default('has been added to your cart') }}</p>
    </div>
</div>