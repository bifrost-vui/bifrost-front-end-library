<p class="m-0 small font-weight-normal badge badge-{{ color|default('primary') }} {{ padding|default('py-1 px-2') }} {{ class|default('') }}">{{ label|default("Here is a simple <strong>badge to showcase what can be done.</strong>")|raw }}</p>