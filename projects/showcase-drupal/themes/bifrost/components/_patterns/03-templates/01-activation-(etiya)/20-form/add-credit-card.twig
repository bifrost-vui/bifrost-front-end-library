<div class="{{ class|default('') }}">

    <div class="d-flex align-items-center mb-4">
        <div><p class="mb-0 mr-3">Cartes de crédits acceptées:</p></div>
        <div class="bg-gray--100 border-rounded px-1 py-1 mr-3 d-flex align-items-center"><img src="/images/logo/credit/mastercard.svg" width="28" alt="Mastercard"></div>
        <div class="bg-gray--100 border-rounded px-1 py-2 d-flex align-items-center justify-content-center"><img src="/images/logo/credit/visa.svg"  width="27" alt="Visa"></div>
    </div>

    {% include '@organisms/20-form/form-credit-card.twig' with { class:"mb-4" }only %}

    <!-- Credit card billing address  -------------------------------- -->
    {% include '@organisms/20-form/form-billing-address-selection--btn-2-choices.twig' only %}
    {#<div class="">#}
        {#{% include '@atoms/03-form/input-with-label.twig' with {#}
            {#class:"mb-2",#}
            {#label:"Adresse",#}
            {#isReadonly: true,#}
            {#inputId:"_payment_address",#}
            {#inputValue:"612 rue Saint-jacques, Montreal, QC, H3C 4M5"#}
        {#} %}#}

        {#<a href="#" class="link">Change address</a>#}
    {#</div>#}

    {#<hr class="my-4">#}
    {% include '@atoms/03-form/input-checkbox-with-label.twig' with {
        class:"mb-0",
        label:'I agree to the <a href="#" class="link">Terms and Conditions for Stored Payment Methods</a>.',
        inputId:"_account_payment_accept-termsconditions"
    } only %}

    {% if hasCheckboxSaveMyCard is defined and hasCheckboxSaveMyCard is same as (true) %}
        <div class="mt-4">
            <p class="font-weight-bold">Sauvegarder ma carte</p>
            {% include '@atoms/03-form/input-checkbox-with-label.twig' with {
                class:"mb-0",
                label:"Enregistrer la carte de credit dans mon compte",
                inputId:"_add_credit_card",
                isChecked: false,
            } %}
        </div>
    {% endif %}

</div>
