import { moduleMetadata }                       from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs/blocks';
import { TwigContainerComponent }               from '../../../../utils/twig-container/twig-container.component';
import interfaceIcons                           from '../public/list/interfaces.json';
import illustrationIcons                        from '../public/list/illustrations.json';
import { IconComponent }                        from '../angular/icon.component';


<Meta
    title='Components/Icon'
    component={IconComponent}
    decorators={[
        moduleMetadata({ declarations: [IconComponent, TwigContainerComponent]})
    ]}
    parameters={{
        design: {
            type: 'figma',
            url: 'https://www.figma.com/file/u25bSG8COM4azJ04CztTv8/DS-Medias?node-id=0%3A1',
            disable: false
        },
    }}
    argTypes={{
        color: {
            defaultValue: '',
            control: {type: 'select'}
        },
        size: {
            defaultValue: 'small',
            type: { required: true },
            table: { defaultValue: { summary: 'small' } },
            control: {type: 'select', options: ['xsmall','small' , 'medium', 'large']}
        },
        icons: {
            table: { disable: true }
        }
    }}
/>


# Icons

<br/>



## Component API

<Props of={IconComponent} />



## Drupal implementation example

``` jsx

{% include '@bf-components/icon/twig/index.twig' with {
    name    : 'account-balance',
    size    : 'xsmall | small | medium | large'
    color   : 'negative',
    class   : 'mr-2'
} only %}

{% include '@bf-components/icon/twig/index.twig' with {
    name    : 'my-account',
    size    : 'xsmall | small | medium | large',
} only %}

```


## Drupal

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});


### Interface Icons

<Preview
    withSource='none'
    style={{ height: '700px' }}
>
    <Story
        name='Drupal - Interface'
        args={{
            elementPath : '../../../icon-library/src/lib/components/icon/_doc/template.drupal',
            icons       : interfaceIcons,
        }}
        argTypes={{
            name: {
                defaultValue: '',
                control: {
                    type: 'select',
                    options: ['All icons', ...interfaceIcons.map(a => a.list).flat().sort()],
                }
            }
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>


### Illustrations Icons

<Preview
    withSource='none'
    style={{ height: '700px' }}
>
    <Story
        name='Drupal - Illustrations'
        args={{
            elementPath : '../../../icon-library/src/lib/components/icon/_doc/template.drupal',
            icons       : illustrationIcons,
        }}
        argTypes={{
            name: {
                defaultValue: '',
                control: {
                    type: 'select',
                    options: ['All icons', ...illustrationIcons.map(a => a.list).flat().sort()],
                }
            }
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>