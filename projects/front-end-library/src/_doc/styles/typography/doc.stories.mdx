import { Canvas, Meta, Story, Preview, Props } from '@storybook/addon-docs';

import HeadingsTwigTemplate             from './headings.drupal.twig';
import ParagraphTwigTemplate            from './paragraphs.drupal.twig';
import InterfaceDisplayTwigTemplate     from './interface-display.drupal.twig';


<Meta 
    title='Styles/Typography'
/>

# Typography Stories

<br/>

## Drupal

export const DrupalHeadings = (args) => ({
    template: HeadingsTwigTemplate(args),
    props: args,
});

<Story 
    name='Drupal - Headings'
>
    {DrupalHeadings.bind({})}
</Story>

## Source Code

```jsx
    <h1>Titlte 1</h1>
```

export const DrupalParagraph = (args) => ({
    template: ParagraphTwigTemplate(args),
    props: args,
});

<Story 
    name='Drupal - Paragraph'
>
    {DrupalParagraph.bind({})}
</Story>


export const DrupalInterfaceDisplay = (args) => ({
    template: InterfaceDisplayTwigTemplate(args),
    props: args,
});

<Story 
    name='Drupal - Interface and Display'
>
    {DrupalInterfaceDisplay.bind({})}
</Story>