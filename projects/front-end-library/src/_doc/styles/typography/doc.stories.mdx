import { moduleMetadata } from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import { TwigContainerComponent }   from '../../../utils/twig-container/twig-container.component';


<Meta 
    title='Styles/Typography'
    decorators={[
        moduleMetadata({ declarations: [TwigContainerComponent]}),
    ]}
    argTypes={{
    }}
/>

# Typography

> We use **Open Sans** for paragraph and **Blender Pro** for Headings.

<br/>

## Drupal

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});

<Preview
    withSource='none'
    style={{ height: '300px' }}
>
    <Story 
        name='Drupal - Headings'
        args={{ 
            elementPath : '../_doc/styles/typography/headings.drupal',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>

<Preview
    withSource='none'
    style={{ height: '300px' }}
>
    <Story 
        name='Drupal - Paragraphs'
        args={{ 
            elementPath : '../_doc/styles/typography/paragraphs.drupal',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>


## Source Code

```php
    <h1>Titlte 1</h1>
```