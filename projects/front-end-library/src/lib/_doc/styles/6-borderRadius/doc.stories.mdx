import { Meta, Canvas }     from '@storybook/addon-docs';
import borderRadius         from './borderRadius';

<Meta
    title='Styles/Border Radius'
    parameters={{
        layout: 'padded',
        viewMode: 'docs',
        previewTabs: { canvas: { hidden: true } }
    }}
/>

# Border Radius

<Canvas withSource='none'>
    <div>
        <div class='d-flex align-items-center gap-4 w-100'>
            <div class='w-65'>
                <h5 class='mb-0'>Name</h5>
            </div>
            <div class='w-35'>
                <h5 class='mb-0'>All Themes</h5>
            </div>
        </div>
        <ul class='list-unstyled'>
            {borderRadius.map(b =>
                <li key={b.name} class='d-flex align-items-center gap-4 w-100 border-top pt-3 mt-3'>
                    <div class='w-65 d-flex align-items-start gap-4'>
                        <h4 class='mb-0' style={{ width: '8rem' }}>
                            {b.name}
                        </h4>
                        <div class='d-flex flex-column'>
                            <code>--bf-border-radius-{b.name}</code>
                            <code>.bf-border-radius-{b.name}</code>
                        </div>
                    </div>
                    <div class='w-35 d-flex align-items-center gap-4'>
                        <p class='bf-text mb-0'>
                            {b.value}
                        </p>
                        <div class={`bf-border-radius-${b.name} border bf-border-color-primary`} style={{ width: '40px', height: '40px' }}></div>
                    </div>
                </li>
            )}
        </ul>
    </div>
</Canvas>