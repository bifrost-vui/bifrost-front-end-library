import { moduleMetadata } from '@storybook/angular';
import { Canvas, Story, Meta } from '@storybook/addon-docs';
import { TwigContainerComponent } from '../../../../utils/twig-container/twig-container.component';

<Meta title="Styles/Headings - Stories" decorators={[moduleMetadata({ declarations: [TwigContainerComponent] })]} />

# Headings Stories

<br />

## Drupal

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});

<Canvas withSource="none">
    <Story
        name="Drupal - Headings"
        args={{
            elementPath: '_doc/styles/3-headings/headings.drupal',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>
