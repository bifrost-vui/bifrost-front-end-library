import { moduleMetadata } from '@storybook/angular';
import { Canvas, Story, Meta } from '@storybook/addon-docs';
import { TwigContainerComponent } from '../../../../utils/twig-container/twig-container.component';

<Meta
    title='Styles/Inputs'
    decorators={[
        moduleMetadata({ declarations: [TwigContainerComponent] }),
    ]}
    parameters={{
        layout: 'padded',
        viewMode: 'docs',
    }}
/>

# Inputs

<br/>

## Drupal
This is an example of a working form with twig component, simulating value sent to back-end with Javascript

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});

<Canvas withSource='none'>
    <Story
        name='Drupal - Inputs'
        args={{
            elementPath : '_doc/styles/8-inputs/inputs.drupal',
            isRequired: false,
            isDisabled: false,
            isInvalid: false,
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>