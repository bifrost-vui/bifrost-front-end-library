import { Story, Meta, Preview, Props } from '@storybook/addon-docs/blocks';
import { linkTo } from '@storybook/addon-links';
import { moduleMetadata } from '@storybook/angular';
import { TileComponent } from '../../utils/tile/tile.component';
import { Stack, Tile } from '../../utils/documentation-style.js';

<Meta
    title='Pipes/All Pipes'
    parameters={{ 
        layout: 'padded', 
        viewMode: 'docs',
        previewTabs: { canvas: { hidden: true } }
    }}
    decorators={[
        moduleMetadata({ declarations: []})
    ]}
/>

# All Pipes

<br/>

<Stack>

<Tile onClick={linkTo('pipes-uppercase--default')}>
    Uppercase
    <iframe title="Default" src="iframe.html?id=pipes-uppercase--default&viewMode=story"></iframe>
</Tile>

</Stack>