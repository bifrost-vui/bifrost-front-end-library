{# Import macros #}
{% import "@bf-utils/macros/twig/index.twig" as macros %}

{# Default variables #}
{% set customTag = customTag|default("div") %}
{% set attr = attr|default({}) %}

{# Template #}
<{{ customTag|raw }} {{ macros.addAttributes(attr) }}>
  {% block customTagContent %}{% endblock %}
</{{ customTag|raw }}>
