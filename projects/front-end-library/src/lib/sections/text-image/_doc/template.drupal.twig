{% set buttons              = buttons|json_parse %}
{% set rowButtons           = rowButtons|json_parse %}
{% set links                = links|json_parse %}
{% set image                = image|json_parse %}
{% set reversed             = reversed|json_parse %}

<section class='d-flex flex-column justify-content-center minh-100'>
    {% for index in [1,2] %}
        <div class='flex-grow-1 d-flex flex-column align-items-center justify-content-center minh-100 {% if index == 2 and reversed is not defined %}reversed bf-color-bg-ground{% endif %}'>
        
            {% include '@bf-sections/text-image/twig/index.twig' %}

        </div>
    {% endfor %}
</section>