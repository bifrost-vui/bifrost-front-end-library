import { moduleMetadata } from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props } from '@storybook/addon-docs';

import { interfaceIcons }                   from '../../icon/_doc/data.ts';
import { ButtonIconComponent }              from '../angular/button-icon.component';

import ButtonIconTwigTemplate               from './template.drupal.twig';
import ButtonIconTwig                       from '../twig/index.twig';



<Meta 
    title='Components/Button Icon' 
    component={ButtonIconComponent}
    argTypes={{
        icon: {
            control: {
                type: 'select',
                options: ['', ...interfaceIcons.map(a => a.list).flat().sort()]
            }
        },
    }}
/>


# Button Icon

<br/>



## Component API

<Props of={ButtonIconComponent} />



## Drupal implementation example

```jsx
{% include '@bf-components/button-icon/twig/index.twig' with {
    icon        : 'placeholder'
} only %}
```



## Drupal

export const Drupal = (args) => ({
    template: ButtonIconTwigTemplate(args),
    props: args,
});

<Preview
    withSource='none'
>
    <Story 
        name='Drupal'
        args={{
            icon        : 'helix-tv'
        }}
        height='160px'
    >
        {Drupal.bind({})}
    </Story>
</Preview>