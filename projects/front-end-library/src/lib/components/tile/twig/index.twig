{% set isLink = href is defined and href is not empty %}

{{ isLink ? "<a" : "<button" }}
{% if isLink is same as(true) %}
  href="{{ href|default("#") }}"
{% else %}
  type="button"
{% endif %}class="bf-tile
{{ reversed is same as(true) ? "reversed" }}
{{ isDisabled is same as(true) ? "disabled" }}
{{ class|default("") }}" style="{{ style|default("") }}"
{{ isDisabled is same as(true) ? "disabled='disabled'" }}
{{ dataDismiss is defined
  ? "data-dismiss='" ~ (dataDismiss|default(null)) ~ "'" }}
{{ dataToggle is defined ? "data-toggle='" ~ (dataToggle|default(null)) ~ "'" }}
{{ dataTarget is defined ? "data-target='" ~ (dataTarget|default(null)) ~ "'" }}
{{ ariaControls is defined
  ? "aria-controls='" ~ (ariaControls|default(null)) ~ "'" }}
{{ ariaExpanded is defined
  ? "aria-expanded='" ~ (ariaExpanded|default(null)) ~ "'" }}
{{ ariaLabel is defined ? "aria-expanded='" ~ (ariaLabel|default(null)) ~ "'" }}
{{ extraAttribute|default(null) }}
>{% block content %}
  {{ content|default("") }}
{% endblock %}
{{ isLink ? "</a" : "</button" }}>
