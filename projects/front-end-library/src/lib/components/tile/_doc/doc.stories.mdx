import { moduleMetadata }                       from '@storybook/angular';
import { ArgsTable, Canvas, Meta, Story }       from '@storybook/addon-docs';
import TwigTemplate                             from './template.drupal.twig';
import TwigAllTemplate                          from './list.drupal.twig';
import { TileComponent }                        from '../angular/tile.component';


<Meta
    title='Components/Tile'
    component={TileComponent}
    decorators={[
        moduleMetadata({ declarations: [TileComponent]})
    ]}
    argTypes={{}}
/>


# Tile

<br/>



## Component API

<ArgsTable of={TileComponent} />



## Drupal implementation example

```jsx
{% include '@bf-components/tile/twig/index.twig' with {
    content : 'Tile label',
} only %}
```



## Drupal

export const Drupal = (args) => ({
    template: TwigTemplate(args),
    props: args,
});

<Canvas withSource='none'>
    <Story
        name='Drupal'
        height='600px'
        args={{
            class           : '',
            content         : `<div class='d-flex flex-column'><img src='/logo/google.svg' /></div>`,
            reversed: undefined,
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>

## Drupal all example

export const DrupalAll = (args) => ({
    template: TwigAllTemplate(args),
    props: args,
});

<Canvas withSource='none'>
    <Story
        name='Drupal - List example'
        height='600px'
        parameters={{ options: { showPanel: false } }}
        args={{
            reversed: undefined,
        }}
    >
        {DrupalAll.bind({})}
    </Story>
</Canvas>