import { moduleMetadata }                       from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs';

import { interfaceIcons }                       from '../../icon/_doc/data.ts';
import { TileComponent }                        from '../angular/tile.component';

import TileTwigTemplate                         from './template.drupal.twig';
import TileListTwigTemplate                     from './template-list.drupal.twig';


<Meta 
    title='Components/Tile' 
    component={TileComponent}
    argTypes={{}}
/>


# Tile

<br/>



## Component API

<Props of={TileComponent} />



## Drupal implementation example

```jsx
{% include '@bf-components/tile/twig/index.twig' with {
    content : 'Tile label',
} only %}
```



## Drupal

export const Drupal = (args) => ({
    template: TileTwigTemplate(args),
    props: args,
});

<Canvas
    withSource='none'
>
    <Story 
        name='Drupal'
        args={{
            class           : '',
            content         : `<div class='d-flex flex-column'>
    <img src='/logo/google.svg' />
</div>`,
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>


export const DrupalList = (args) => ({
    template: TileListTwigTemplate(args),
    props: args,
});

<Canvas
    withSource='none'
>
    <Story 
        name='Drupal - List example'
    >
        {DrupalList.bind({})}
    </Story>
</Canvas>