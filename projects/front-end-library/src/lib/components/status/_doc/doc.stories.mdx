import { moduleMetadata }                   from '@storybook/angular';
import { ArgsTable, Canvas, Meta, Story }   from '@storybook/addon-docs';
import { withDesign }                       from 'storybook-addon-designs';
import TwigTemplate                         from './template.drupal.twig';
import { StatusComponent }                  from '../angular/status.component';

<Meta
    title='Components/Status'
    component={StatusComponent}
    decorators={[
        moduleMetadata({ declarations: [StatusComponent]}),
        withDesign
    ]}
    argTypes={{
        label: {
            type: { required: true }
        },
        color: {
            control: { type: 'select', options: ['', 'progress', 'success' , 'unknown', 'failed'] }
        },
        _reversed: {
            control: {type: 'boolean' },
            description: '**[ ! ]** This control display as testing purposed. **[ ! ]**',
            table: { defaultValue: { summary: false } }
        }
    }}
    parameters={{
        design: {
            type: 'figma',
            url: 'https://www.figma.com/file/7bTmk29aCBl7rCFszffYPr/DS---Components-%26-styles?node-id=10083%3A668'
        },
    }}
/>

# Status
[Design Documentation](https://zeroheight.com/79f1d3365/) not available

The component is display on all background as example, but it should be set or background highlight as shown in Figma

## Component API
<ArgsTable of={StatusComponent} />

## Drupal implementation example

```jsx
{% include '@bf-components/status/twig/index.twig' with {
    color : 'success',
    label : 'item name',
    class : 'mx-2'
} only %}
```

## Drupal

export const Drupal = (args) => ({
  template: TwigTemplate(args),
  props: args,
});

<Canvas withSource='none'>
    <Story
        name='Drupal'
        height='100px'
        args={{
            label       : '',
            color       : '',
            reversed    : false
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>

## Design Guidelines
<iframe width='100%' height='450' src='https://www.figma.com/embed?embed_host=share&url=https://www.figma.com/file/7bTmk29aCBl7rCFszffYPr/DS---Components-%26-styles?node-id=10083%3A668' frameBorder='0' allowFullScreen></iframe>