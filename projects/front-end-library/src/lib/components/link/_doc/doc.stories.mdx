import { moduleMetadata }                       from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs/blocks';

import LinkTwigTemplate                         from './template.drupal.twig';


<Meta 
    title='Components/Link'
    argTypes={{}}
/>


# Link

<br/>


## Drupal

> There is no Link Drupal component in Bifrost.
> Just, use Bifrost classes <b>.bf-link</b>.

export const Drupal = (args) => ({
    template: LinkTwigTemplate(args),
    props: args,
});


<Canvas
    withSource='none'
>
    <Story 
        name='Drupal'
    >
        {Drupal.bind({})}
    </Story>
</Canvas>