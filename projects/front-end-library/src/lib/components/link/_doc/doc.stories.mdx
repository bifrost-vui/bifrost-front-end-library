import { moduleMetadata }                       from '@storybook/angular';
import { ArgsTable, Canvas, Meta, Story }       from '@storybook/addon-docs';
import interfaceIcons                           from '../../../../../../../public/icons/list/interfaces.json';
import TwigTemplate                             from './template.drupal.twig';
import TwigCSSTemplate                          from './template-css-only.drupal.twig';
import TwigExampleTemplate                      from './template-examples.drupal.twig';
import { LinkComponent }                        from '../angular/link.component';


<Meta
    title='Components/Link'
    component={LinkComponent}
    decorators={[
        moduleMetadata({ declarations: [LinkComponent]})
    ]}
    argTypes={{
        hierarchy: {
            control: { type: 'select' },
            table: { defaultValue: { summary: 'native' } },
        },
        iconName: {
            control: {
                type: 'select',
                options: ['', 'placeholder', ...interfaceIcons.map(a => a.list).flat().sort()]
            }
        },
        iconPosition: {
            table: { defaultValue: { summary: 'left' } },
        },
        isUnderline: {
            table: { defaultValue: { summary: true } },
        },
        isDisabled: {
            table: { defaultValue: { summary: false } },
        },
    }}
/>


# Link


## Component API

<ArgsTable of={LinkComponent} />


## Drupal implementation example

```jsx
// Link native (default)
{% include '@bf-components/link/twig/index.twig' with {
    label       : 'Link native',
} only %}

// Link primary
{% include '@bf-components/link/twig/index.twig' with {
    label       : 'Link primary',
    hierarchy   : 'primary'
} only %}

// Or use .bf-link class
<a href='/' class='bf-link'>Link native</a>
<a href='/' class='bf-link bf-link--primary'>Link primary</a>
<a href='/' class='bf-link disabled' aria-disabled='true' role='link'>Link disabled</a>
```


## Drupal

export const Drupal = (args) => ({
    template: TwigTemplate(args),
    props: args,
});

<Canvas withSource='none'>
    <Story
        name='Drupal'
        height='600px'
        args={{
            class       : 'mb-3',
            href        : 'https://example.com',
            reversed: undefined,
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>

## Drupal Examples

export const DrupalExample = (args) => ({
    template: TwigExampleTemplate(args),
    props: args,
});

<Canvas withSource='none'>
    <Story
        name='Drupal - Examples'
        height='600px'
        parameters={{ options: { showPanel: false } }}
        args={{
        }}
    >
        {DrupalExample.bind({})}
    </Story>
</Canvas>

## Drupal CSS only

export const DrupalCSS = (args) => ({
    template: TwigCSSTemplate(args),
    props: args,
});

<Canvas withSource='none'>
    <Story
        name='Drupal - CSS Only'
        parameters={{ options: { showPanel: false } }}
        args={{
            reversed: undefined,
        }}
    >
        {DrupalCSS.bind({})}
    </Story>
</Canvas>