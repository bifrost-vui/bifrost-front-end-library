import { moduleMetadata }                       from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs/blocks';

import { TwigContainerComponent }               from '../../../../utils/twig-container/twig-container.component';
import { BlockHighlightComponent }              from '../angular/block-highlight.component';
import { commonProps }                          from './doc.stories.mdx';


<Meta 
    title='Components/Block Highlight/Composition'
    component={BlockHighlightComponent}
    decorators={[
        moduleMetadata({ declarations: [BlockHighlightComponent, TwigContainerComponent]}),
    ]}
/>

# Block Highlight Composition


## Drupal

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});


<Preview
    withSource='none'
    style={{ height: '800px' }}
>
    <Story 
        name='Drupal - Composition'
        args={{
            ...commonProps,
            elementPath : 'components/block-highlight/_doc/composition.drupal',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>