import { moduleMetadata }                       from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs/blocks';

import { TwigContainerComponent }               from '../../../../utils/twig-container/twig-container.component';
import { BlockHighlightComponent }              from '../angular/block-highlight.component';


<Meta 
    title='Components/Block Highlight' 
    component={BlockHighlightComponent}
    decorators={[
        moduleMetadata({ declarations: [BlockHighlightComponent, TwigContainerComponent]}),
    ]}
    argTypes={{
        titleTag: {
            table: { defaultValue: { summary: 'p' } },
        },
        blockBackgroundColor: {
            table: { defaultValue: { summary: 'ground' } },
            control: {
                type: 'select',
                defaultValue: 'ground'
            }
        },
        variant: {
            table: { disable: true }
        }
    }}
/>

# Block Highlight

<br/>



## Component API

<Props of={BlockHighlightComponent} />



## Drupal implementation example

### Promo

```jsx
{% include '@bf-components/block-highlight/twig/promo.twig' with {
    image                   : {
        src : '/images/assets/helix-equipment.png'
    },
    visualBackgroundColor   : '#ff00ff',
    blockBackgroundColor    : 'underground',
    title                   : 'All the mobile accessories',
    titleTag                : 'h2',
    paragraph               : 'Find the perfect accessories for your device.',
    button                  : {
        label : 'Shop',
        class : ''
    },
    reversed                : false,
    class                   : ''
} only %}
```

### Featured

```jsx
{% include '@bf-components/block-highlight/twig/featured.twig' with {
    // Component API is exaclty the same for promo and featured.
} only %}
```


export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});

export const commonProps = {
    elementPath         : 'components/block-highlight/_doc/template.drupal',
    image               : {
        src : window.location.origin + '/images/placeholder/800x600.png',
        // src: 'https://i.stack.imgur.com/y9DpT.jpg'
    },
    title               : 'All the mobile accessories',
    paragraph           : 'Find the perfect accessories for your device.',
    button              : {
        label   : 'Shop',
        class   : ''
    },
    class               : ''
}


## Variant Featured

<Preview
    withSource='none'
    style={{ height: '800px' }}
>
    <Story 
        name='Drupal - Featured'
        args={{
            ...commonProps,
            variant : 'featured',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>


## Variant Promo

<Preview
    withSource='none'
    style={{ height: '800px' }}
>
    <Story 
        name='Drupal - Promo'
        args={{
            ...commonProps,
            variant : 'promo',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>