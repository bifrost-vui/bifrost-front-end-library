import { moduleMetadata }                       from '@storybook/angular';
import { ArgsTable, Canvas, Meta, Story }       from '@storybook/addon-docs';
import { BlockHighlightComponent }              from '../angular/block-highlight.component';
import TwigTemplate                             from './template.drupal.twig';
import TwigTemplateExample                      from './template-usage-example.drupal.twig';

<Meta
    title='Components/Block Highlight'
    component={BlockHighlightComponent}
    decorators={[
        moduleMetadata({ declarations: [BlockHighlightComponent]}),
    ]}
    argTypes={{
        titleTag: {
            table: { defaultValue: { summary: 'p' } },
        },
        blockBackgroundColor: {
            table: { defaultValue: { summary: 'ground' } },
            control: {
                type: 'select',
                defaultValue: 'ground'
            }
        },
        isVisualBackgroundFill: {
            table: { defaultValue: { summary: true } },
        }
    }}
/>

# Block Highlight

> 👉 &nbsp; Please, read [Full Documentation about Block-Highlight](https://zeroheight.com/79f1d3365/p/688590-block-highlight).

<br/>

## Component API

<ArgsTable of={BlockHighlightComponent} />



## Drupal implementation example

```jsx
{% include '@bf-components/block-highlight/twig/index.twig' with {
    image                   : {
        src : '/images/assets/helix-equipment.png'
    },
    visualBackgroundColor   : '#ff00ff',
    blockBackgroundColor    : 'underground',
    title                   : 'All the mobile accessories',
    titleTag                : 'h2',
    paragraph               : 'Find the perfect accessories for your device.',
    button                  : {
        label : 'Shop',
        class : ''
    },
    isFeatured              : false,
    reversed                : false,
    class                   : ''
} only %}
```


export const Drupal = (args) => ({
    template: TwigTemplate(args),
    props: args,
});

export const DrupalUsageExample = (args) => ({
template: TwigTemplateExample(args),
props: args,
});

export const commonProps = {
    image               : {
        src : window.location.origin + '/images/placeholder/800x600.png',
        // src: 'https://i.stack.imgur.com/y9DpT.jpg'
    },
    title               : 'All the mobile accessories',
    paragraph           : 'Find the perfect accessories for your device.',
    button              : {
        label   : 'Shop',
        class   : ''
    },
    isFeatured          : false,
    class               : ''
}

<Canvas withSource='none'>
    <Story
        name='Drupal'
        height='800px'
        args={{
            ...commonProps,
            reversed: undefined,
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>


<Canvas withSource='none'>
    <Story
        name='Drupal - Usage example'
        height='800px'
        parameters={{ options: { showPanel: false } }}
        args={{
            ...commonProps,
            image: undefined,
            reversed: undefined,
        }}
    >
        {DrupalUsageExample.bind({})}
    </Story>
</Canvas>