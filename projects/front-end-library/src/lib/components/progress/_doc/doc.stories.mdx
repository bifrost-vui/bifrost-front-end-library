import { moduleMetadata }               from '@storybook/angular';
import { Canvas, Meta, Story, Props }   from '@storybook/addon-docs';
import { withDesign }                   from 'storybook-addon-designs'
import { TwigContainerComponent }       from '../../../../utils/twig-container/twig-container.component';
import { ProgressComponent }            from '../angular/progress.component';

<Meta
    title='UI-Kit/Progress'
    component={ProgressComponent}
    decorators={[
        moduleMetadata({ declarations: [ProgressComponent, TwigContainerComponent]}),
        withDesign
    ]}
    parameters={{
        design: {
            type    : 'figma',
            url     : 'https://www.figma.com/file/7bTmk29aCBl7rCFszffYPr/DS---Components-%26-styles?node-id=3182%3A13',
            disable : false
        },
    }}
    argTypes={{
        _background :{
            control : {
                type : 'select',
                options : ['', 'bf-color-bg-ground','bf-color-bg-underground','reversed bf-color-bg-ground','reversed bf-color-bg-underground', 'bf-color-bg-highlight']
            },
            description : 'Preview the component on selected background'
        }
    }}
/>

# Progress

This component has been migrated from the UI-Kit project without any optimisation

**Progress bar Colors**

- when `progressPercentValue` > 90 : color = danger (red)
- when `progressPercentValue` > 50 : color = warning (orange)
- when `progressPercentValue` =< 50 : color = success (green)
- when `isMonochromeColor` is true, the progression is always yellow, no regards to the progression percentage


## Drupal

export const Drupal = (args) => ({
    component   : TwigContainerComponent,
    props       : args,
});

<Canvas
    withSource='none'
    style={{height: '400px'}}
>
    <Story
        name='Drupal'
        args={{
            elementPath          : 'components/progress/_doc/template.drupal',
            progressValue        : 20,
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>


## Drupal implementation example

```jsx
{% include '@bf-components/progress/twig/index.twig' with {
    progressValue   : 50,
    progressPercentValue : 50 // Calculations must be made outside the component
} only %}
```


## Component API

**✅ Inputs are equivalent for Angular and Drupal.**

<Props of={ProgressComponent} />


## Design Guidelines


<iframe width='100%' height='450' src='https://www.figma.com/embed?embed_host=share&url=https%3A%2F%2Fwww.figma.com%2Ffile%2F7bTmk29aCBl7rCFszffYPr%2FDS---Components-%2526-styles%3Fnode-id%3D3182%253A13' frameBorder='0' allowFullScreen></iframe>