import { moduleMetadata }                       from '@storybook/angular';
import { ArgsTable, Canvas, Meta, Story }       from '@storybook/addon-docs';
import illustrationIcons                        from '../../../../../../../public/icons/list/illustrations.json';
import TwigTemplate                             from './template.drupal.twig';
import { ButtonNavComponent }                   from '../angular/button-nav.component';

<Meta
    title='Components/Button Nav'
    component={ButtonNavComponent}
    decorators={[
        moduleMetadata({ declarations: [ButtonNavComponent]})
    ]}
    argTypes={{
        iconName: {
            table: { defaultValue: { summary: 'placeholder' } },
            control: {
                type: 'select',
                options: ['', 'placeholder', ...illustrationIcons.map(a => a.list).flat().sort()]
            }
        },
        isFeatured: {
            table: { defaultValue: { summary: false } }
        },
        isDisabled: {
            table: { defaultValue: { summary: false } }
        },
        highlight: {
            table: { defaultValue: { summary: false } }
        },
        reversed: {
            table: { defaultValue: { summary: false } }
        }
    }}
/>

# Button Nav

<br/>

## Component API

<ArgsTable of={ButtonNavComponent} />

## Drupal implementation example

```jsx
{% include '@bf-components/button-nav/twig/index.twig' with {
    label: 'Label',
    description: 'Description',
    iconName: 'placeholder',
    class: 'mt-3',
    reversed: false,
    highlight: false,
} only %}
```

## Drupal

export const Drupal = (args) => ({
    template: TwigTemplate(args),
    props: args,
});

<Canvas withSource='none'>
    <Story
        name='Drupal'
        args={{
            label: 'Label',
            reversed: undefined,
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>

<Canvas withSource='none'>
    <Story
        name='Drupal - With description'
        args={{
            label: 'Label',
            description: 'Description morbi leo risus, porta ac consectetur ac, vestibulum at eros. Morbi leo risus, porta ac consectetur ac, vestibulum at eros.',
            reversed: undefined,
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>

<Canvas withSource='none'>
    <Story
        name='Drupal - With isFeatured'
        args={{
            label: 'Label',
            description: 'Description morbi leo risus, porta ac consectetur ac, vestibulum at eros. Morbi leo risus, porta ac consectetur ac, vestibulum at eros.',
            isFeatured: true,
            reversed: undefined,
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>