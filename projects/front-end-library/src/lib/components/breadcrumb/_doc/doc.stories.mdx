import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs';

import { BreadcrumbComponent }                  from '../angular/breadcrumb.component';
import BreadcrumbTwigTemplate                   from './template.drupal.twig';


<Meta
    title='Components/Breadcrumb' 
    component={BreadcrumbComponent}
/>


# Breadcrumb

<br/>



## Drupal implementation example

```jsx
<nav aria-label='breadcrumb'>
    <ol class='breadcrumb'>
        <li class='breadcrumb-item'><a href='#'>Home</a></li>
        <li class='breadcrumb-item active' aria-current='page'>Library</li>
    </ol>
</nav>
```



## Drupal

> There is no Breadcrumb Drupal component in Bifrost.
> Please use <a class='bf-link' target='_blank' href='https://getbootstrap.com/docs/4.0/components/breadcrumb/'>Bootstrap syntax</a>.


export const Drupal = (args) => ({
    template: BreadcrumbTwigTemplate(args),
    props: args,
});

<Canvas
    withSource='none'
>
    <Story 
        name='Drupal'
    >
        {Drupal.bind({})}
    </Story>
</Canvas>