{# ----------------------------------------------------------------- #}
{% set reversed = reversed|json_parse|default(false) %}

<div
    class="d-flex align-items-center justify-content-center h-100 w-100 {% if reversed %}reversed bf-color-bg-ground{% endif %}"
>

    {# ----------------------------------------------------------------- #}
    {% include '@bf-components/tag/twig/index.twig' with {
        label       : label,
        variant     : variant,
    } only %}

</div>