import { moduleMetadata }                       from '@storybook/angular';
import { Canvas, Meta, Story, Props }           from '@storybook/addon-docs';
import { TwigContainerComponent }               from '../../../../utils/twig-container/twig-container.component';
import { TileKSPComponent }                     from '../angular/tile-ksp.component';


<Meta
    title='Components/Tile KSP'
    component={TileKSPComponent}
    decorators={[
        moduleMetadata({ declarations: [TileKSPComponent, TwigContainerComponent]})
    ]}
    argTypes={{
        isClickable: {
            table: { defaultValue: { summary: true } }
        },
        titleTag: {
            table: { defaultValue: { summary: 'p' } }
        },
        backgroundColor: {
            table: { defaultValue: { summary: 'underground' } }
        }
    }}
/>


# Tile KSP

<br/>



## Component API

<Props of={TileKSPComponent} />



## Drupal implementation example

```jsx
{% include '@bf-components/tile-ksp/twig/index.twig' with {
    href            : 'https://example.com',
    title           : 'Television',
    description     : 'Find all your entertainment in one place.',
    linkLabel       : 'Mode details',
    icon            : {
        name    : 'television',
        color   : 'neutral'
    },
    backgroundImage : {
        src     : '/images/_docs/hero-helix-reversed.png'
    },
    isClickable     : true,
    class           : '',
} only %}
```



## Drupal

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});

export const commonProps = {
    href            : 'https://example.com',
    title           : 'Television',
    description     : 'Cras justo odio, dapibus ac facilisis in, egestas eget quam. Etiam porta sem malesuada magna mollis euismod.',
    linkLabel       : 'Mode details',
    icon            : 'television',
    isClickable     : true,
    class           : '',
}

<Canvas withSource='none'>
    <Story
        name='Drupal'
        height='600px'
        args={{
            elementPath: 'components/tile-ksp/_doc/template.drupal',
            ...commonProps
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>

<Canvas withSource='none'>
    <Story
        name='Drupal - With backgroundImage'
        height='600px'
        args={{
            elementPath: 'components/tile-ksp/_doc/template.drupal',
            ...commonProps,
            icon            : {
                name    : 'television',
                color   : 'neutral'
            },
            backgroundImage: {
                src: '/images/_docs/hero-helix-reversed.png'
            },
            reversed: true
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>