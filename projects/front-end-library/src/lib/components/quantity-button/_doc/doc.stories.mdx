import { moduleMetadata }               from '@storybook/angular';
import { Meta, Story, Preview, Props }  from '@storybook/addon-docs/blocks';
import { withDesign }                   from 'storybook-addon-designs'
import { TwigContainerComponent }       from '../../../../utils/twig-container/twig-container.component';
import { QuantityButtonComponent }     from '../angular/quantity-button.component';

<Meta 
    title='Components/Quantity Button' 
    component={QuantityButtonComponent}
    decorators={[
        moduleMetadata({ declarations: [QuantityButtonComponent, TwigContainerComponent]}),
        withDesign
    ]}
    parameters={{
        design: {
            type    : 'figma',
            url     : 'https://www.figma.com/file/7bTmk29aCBl7rCFszffYPr/DS-Library-UI?node-id=91%3A0',
            disable : false
        },
    }}
    argTypes={{
        min: {
            defaultValue: '1',
            description: 'must be a number',
        },
        max: {
            defaultValue: '3',
            description: 'must be a number',
        }
    }}
/>

# Quantity Button

**// TODO** Write a description of your component.
> **// TODO**: Add a link to component in **[Design Documentation](https://zeroheight.com/79f1d3365/p/70ccb4--buttons)**


## Drupal

export const Drupal = (args) => ({
    component   : TwigContainerComponent,
    props       : args,
});

<Preview
    withSource='none'
    style={{ height: '300px' }}
>
    <Story 
        name='Drupal'
        args={{ 
            elementPath : 'components/quantity-button/_doc/template.drupal',
            label       : 'Quantity Button',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>


## Drupal implementation example

```php
{% include '@bf-components/quantity-button/twig/index.twig' with {
   button  : {
      min  : 0,
      max  : 3,
   }
   reversed            : false,
   class               : ''
} only %}
```


## Angular

export const Angular = (args) => ({
  component: QuantityButtonComponent,
  props: args,
});

<Preview
    withSource='none'
>
    <Story 
        name='Angular' 
        args={{ label: 'Quantity Button' }}
    >
        {Angular.bind({})}
    </Story>
</Preview>

## Angular implementation example

Please, read the [Implementation Page](/?path=/docs/implementation-angular--page).


**// TODO**: Add Stories (variants of your component) as much as you want.


## Component API

**// TODO**: Add a sentence to clarify if API is consistent between Angular and Drupal.

**✅ Inputs are equivalent for Angular and Drupal.**

<Props of={QuantityButtonComponent} />


## Design Guidelines


<iframe width='100%' height='450' src='https://www.figma.com/file/7bTmk29aCBl7rCFszffYPr/DS-Library-UI?node-id=91%3A0' frameBorder='0' allowFullScreen> </iframe>

[Lien Figma](https://www.figma.com/file/7bTmk29aCBl7rCFszffYPr/DS-Library-UI?node-id=91%3A0)