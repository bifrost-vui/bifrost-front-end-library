<i class="icon icon-step-number {{ class|default('d-block') }}"
   data-number="{{ step|default(1) }}"
   {% if dataToggle is defined %}data-toggle="{{ dataToggle|default(NULL) }}"{% endif %}
   {% if dataTarget is defined %}data-target="{{ dataTarget|default(NULL) }}"{% endif %}></i>