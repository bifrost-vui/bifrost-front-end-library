import { moduleMetadata } from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs'

import { icons } from './data';
import { TwigContainerComponent }   from '../../../../utils/twig-container/twig-container.component';
import { IconComponent }     from '../angular/icon.component';


<Meta 
    title='Components/Icon' 
    component={IconComponent}
    decorators={[
        moduleMetadata({ declarations: [IconComponent, TwigContainerComponent]}),
        withDesign
    ]}
    parameters={{
        design: {
            type: 'figma',
            url: 'https://www.figma.com/file/u25bSG8COM4azJ04CztTv8/DS-Medias?node-id=0%3A1',
            disable: false
        },
    }}
    argTypes={{
        color: {
            defaultValue: '',
            control: {
                type: 'select',
                options: ['', 'success', 'neutral', 'warning', 'negative'],
            }
        },
        size: {
            defaultValue: '',
            type: { required: false },
            table: { defaultValue: { summary: 'small' } },
            control: {
                type: 'select',
                options: ['', 'xsmall', 'small', 'medium', 'large'],
            }
        },
        name: {
            defaultValue: '',
            control: {
                type: 'select',
                options: ['All icons', ...icons],
            }
        },
        icons: {
            table: { disable: true }
        }
    }}
/>


# Icons

> Explore **[Design Documentation](https://zeroheight.com/79f1d3365/p/70ccb4--buttons)** (Zeroheight)

<br/>


## Drupal

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});

<Preview>
    <Story 
        name='Drupal'
        args={{ 
            elementPath : 'components/icon/_doc/template.drupal',
            icons,
        }}
        height='500px'
    >
        {Drupal.bind({})}
    </Story>
</Preview>


## Source Code

```php
{% include '@bf-components/icon/twig/index.twig' with {
    name    : 'svg-error',
    color   : 'negative',
    size    : 'medium'
} only %}
```


## Component API

<Props of={IconComponent} />


## Design Guidelines

<iframe width='100%' height='450' src='https://www.figma.com/file/u25bSG8COM4azJ04CztTv8/DS-Medias' frameBorder='0' allowFullScreen></iframe>