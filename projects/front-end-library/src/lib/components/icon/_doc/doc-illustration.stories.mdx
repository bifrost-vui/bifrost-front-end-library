import { moduleMetadata }                       from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs/blocks';

import { illustrationIcons }        from './data';
import { TwigContainerComponent }   from '../../../../utils/twig-container/twig-container.component';
import { IconComponent }            from '../angular/icon.component';


<Meta
    title='Components/Icon'
    component={IconComponent}
    decorators={[
        moduleMetadata({ declarations: [IconComponent, TwigContainerComponent]}),
    ]}
    parameters={{
        design: {
            type: 'figma',
            url: 'https://www.figma.com/file/u25bSG8COM4azJ04CztTv8/DS-Medias?node-id=0%3A1',
            disable: false
        },
    }}
    argTypes={{
        color: {
            defaultValue: '',
            description: 'success | neutral | warning | negative',
            control: {
                type: 'select',
                options: ['', 'success', 'neutral', 'warning', 'negative'],
            }
        },
        class:{
            defaultValue:'',
            type: { required: false },
            description: 'use the *svg-fill* class when using filled icons with *colors*'
        },
        size: {
            defaultValue: 'large',
            type: { required: true },
            description: 'preferably use *medium* 48px or *large* 72px',
            table: { defaultValue: { summary: 'large' } },
            control: {
                type: 'select',
                options: ['xsmall','small', 'medium', 'large']
            }
        },
        name: {
            defaultValue: '',
            control: {
                type: 'select',
                options: ['All icons', ...illustrationIcons.map(a => a.list).flat().sort()],
            }
        },
        icons: {
            table: { disable: true }
        },
        type: {
            defaultValue: 'illustration',
            type: { required: true },
            control: {
              readOnly: true
            },
            table: { defaultValue: { summary: 'illustration' } },
        },
    }}
/>


# Icons

<br/>



## Component API

<Props of={IconComponent} />



## Drupal

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});

<Preview
    withSource='none'
    style={{ height: '700px' }}
>
    <Story
        name='Drupal - Illustration'
        args={{
            elementPath : 'components/icon/_doc/template.drupal',
            icons       : illustrationIcons,
            type        : 'illustration',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>
