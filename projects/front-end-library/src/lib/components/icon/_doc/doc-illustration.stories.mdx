import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs';

import { illustrationIcons }        from './data';
import { IconComponent }            from '../angular/icon.component';
import IconTwigTemplate             from './template.drupal.twig';


<Meta 
    title='Components/Icon'
    component={IconComponent}
    parameters={{
        design: {
            type: 'figma',
            url: 'https://www.figma.com/file/u25bSG8COM4azJ04CztTv8/DS-Medias?node-id=0%3A1',
            disable: false
        },
    }}
    argTypes={{
        color: {
            description: 'success | neutral | warning | negative',
            control: {
                type: 'select',
                options: ['', 'success', 'neutral', 'warning', 'negative'],
            }
        },
        size: {
            type: { required: false },
            table: { defaultValue: { summary: 'large' } },
            control: {
                type: 'select',
                options: ['', 'medium', 'large'],
            }
        },
        name: {
            control: {
                type: 'select',
                options: ['All icons', ...illustrationIcons.map(a => a.list).flat().sort()],
            }
        },
        icons: {
            table: { disable: true }
        },
        type: {
            control: {
                readOnly: true
            },
            table: { defaultValue: { summary: 'interface' } },
        },
    }}
/>


# Icons

<br/>



## Component API

<Props of={IconComponent} />



## Drupal

export const Drupal = (args) => ({
    template: IconTwigTemplate(args),
    props: args,
});

<Preview
    withSource='none'
    style={{ height: '700px' }}
>
    <Story 
        name='Drupal - Illustration'
        args={{
            icons       : illustrationIcons,
            type        : 'illustration',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>