import { moduleMetadata }                       from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs/blocks';

import { InputSearchComponent }                 from '../angular/input-search.component';

import InputSearchTwigTemplate                  from './template.drupal.twig';


<Meta 
    title='Components/Input Search' 
    component={InputSearchComponent}
/>


# Input Search

<br/>



## Component API

<Props of={InputSearchComponent} />



## Drupal implementation example

```jsx
{% include '@bf-components/input-search/twig/index.twig' with {
    class                   : 'class',
    reversed                : true,
    buttonClass             : 'buttonClass',
    iconClass               : 'iconClass',
    invalidMsg              : 'invalidMsg',
    isDisabled              : false,
    input                   : {
        id                      : 'id',
        class                   : 'class',
        iconPosition            : 'right',
        type                    : 'text',
        placeholder             : 'Placeholder',
        name                    : 'Name',
        value                   : 'Value',
        extraAttribute          : 'extraAttribute',
    },
    inputSubmit             : {
        id                      : 'id',
        value                   : 'value',
        extraAttribute          : 'extraAttribute',
    },
    label                   : {
        value                   : 'value',
        class                   : 'class',
        extraAttribute          : 'extraAttribute',
    },
    mtValue                 : mtValue,
} only %}
```



## Drupal

export const Drupal = (args) => ({
    template: InputSearchTwigTemplate(args),
    props: args,
});

<Preview
    withSource='none'
>
    <Story 
        name='Drupal'
        args={{ 
            label_value         : 'Label',
            input_placeholder   : 'What are you looking for?',
            isInvalid           : false,
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>
