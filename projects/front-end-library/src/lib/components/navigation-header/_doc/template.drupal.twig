
{% include '@bf-components/navigation-header/twig/index.twig' with {
    hasTopBar               : hasTopBar|default(true)|json_parse,
    topBar                  : {
        items                   : topBar_items|default([])|json_parse,
        secondaryItems          : topBar_secondaryItems|default([])|json_parse,
    },
    logo                    : {
        name                    : logo_name|default('')
    },
    navItems                : navItems|default([])|json_parse,
    hasMiniCartBtn          : hasMiniCartBtn|default(false)|json_parse,
    hasNoSearchBtn          : hasNoSearchBtn|default(false)|json_parse,
    reversed                : reversed|default(false)|json_parse,
    langcode                : langcode|default('EN')
} only %}