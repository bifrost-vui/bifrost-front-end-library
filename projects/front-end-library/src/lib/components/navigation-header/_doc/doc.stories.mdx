import { moduleMetadata } from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import { topBar_items, topBar_secondaryItems, rigthIconLinks, cartBtn, funnelNav } from './data';
import headerLogos from './headerLogos.json';
import { TwigContainerComponent }   from '../../../../utils/twig-container/twig-container.component';
import { HeaderComponent }          from '../angular/header.component';


<Meta
    title='Components/Navigation Header'
    component={HeaderComponent}
    decorators={[
        moduleMetadata({ declarations: [HeaderComponent, TwigContainerComponent]})
    ]}
    argTypes={{
        accountHello: {
            table: { defaultValue: { summary: 'Hello' } },
        },
        langcode: {
            defaultValue: 'FR',
            control: {
                type: 'select',
                options: ['EN', 'FR'],
            }
        },
        logoName: {
            defaultValue: 'Videotron',
            control: {
                type: 'select',
                options: [...headerLogos.map(item => item.name)],
            }
        },
        hasTopBar: {
            defaultValue: true,
        },
        reversed: {
            defaultValue: false,
        },
        headerLogos: {
            table: { disable: true }
        }
    }}
/>


# Header

<br/>

## Component API

<Props of={HeaderComponent} />



## Drupal implementation example

### Main Navigation Header
```jsx
{% include '@bf-components/navigation-header/twig/index.twig' with {
    hasTopBar: true,
    topBar: {
        items: [
            { label: 'Personal',    href: '', class: 'active',  },
            { label: 'Business',    href: '', class: '' },
        ],
        secondaryItems: [
            { label: 'Webmail',     href: '', class: '' },
            { label: 'illico.tv',   href: '', class: '' },
            { label: 'Locate',      href: '', class: '' }
        ],
    },
    logoName: 'videotron',
    logoLink: 'https://videotron.com',
    navItems: [
        {
            label: 'Shop', href: '', class: '',
            subItemsMultiDropdown: [
                {
                    label : 'Helix plans', href : '', class : '',
                    listItems: [
                        { label : 'The Helix experience',   href : '', class : '' },
                        { label : 'Helix TV',               href : '', class : '' },
                        { label : 'Helix Internet',         href : '', class : '' },
                    ]
                },
                {
                    label : 'Other custom bundles', href : '', class : '',
                    listItems: []
                },
            ],
            mega: [
                { imgSrc: 'https://source.unsplash.com/400x250/?nature', imgAlt: 'Thumbnail', href: '#', title: '', text: '' },
                { imgSrc: 'https://source.unsplash.com/400x250/?nature', imgAlt: 'Thumbnail', href: '#', title: '', text: '' },
            ]
        },
        {
            label : 'Support', href : '#', class : '',
            subItemsMultiDropdown: [...],
            mega: [...],
        }
        ...
    ],
    rigthIconLinks : [
        { icon : 'email',   href : ' ', class : '' },
        { icon : 'pin',     href : ' ', class : '' },
    ],
    hasCartBtn : true,
    cartBtn : {
        badge       : { label : '3' },
        href        : 'https://www.videotron.com',
        ariaLabel   : 'My cart'
    },
    hasAccountBtn : true,
    isConnected : true,
    hrefLogOut : '#',
    labelLogOut : 'Log out',
    hrefConnexion : '#',
    labelConnexion : 'Connexion',
    search : {
        placeholder : 'What are you looking for?'
    },
    reversed : true,
    langcode : 'EN'
} only %}
```
### Funnel type Navigation Header

```jsx
{% include '@bf-components/navigation-header/twig/index.twig' with {
    logoName    : 'videotron',
    logoLink    : 'https://videotron.com',
    hasCartBtn  : true,
    cartBtn     : {
        badge       : { label : '3' },
        href        : 'https://www.videotron.com',
        ariaLabel   : 'My cart'
    },
    funnelNav   : {
        label   : "Back" ,
        href    : " ",
    },
    reversed    : true,
    langcode    : 'EN'
} only %}
```

### Navigation with custom logo image

```jsx
{% enmbed '@bf-components/navigation-header/twig/index.twig' with {
    logoLink    : 'https://videotron.com',
    navItems: [...],
    rigthIconLinks : [
        { icon : 'email',   href : ' ', class : '' },
        { icon : 'pin',     href : ' ', class : '' },
    ],
    hasCartBtn : false,
    hasAccountBtn : true,
    isConnected : true,
    hrefLogOut : '#',
    labelLogOut : 'Log out',
    hrefConnexion : '#',
    labelConnexion : 'Connexion',
    search : {
        placeholder : 'What are you looking for?'
    },
    reversed : true,
    langcode : 'EN'
} only %}
    {% block headerLogo %}
        <img src="/dir/my-logo-img.svg" alt="alternate text" aria-roledescription="logo" role="img" />
    {% endblock %}
{% endembed %}
```



## Drupal

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});


### Main Navigation Header

<Preview
    withSource='none'
>
    <Story
        name='Navigation Header - Drupal'
        args={{
            elementPath : 'components/navigation-header/_doc/template.drupal',
            topBar_items,
            topBar_secondaryItems,
            rigthIconLinks,
            cartBtn,
            headerLogos
        }}
        height='400px'
        with = '1200px'
    >
        {Drupal.bind({})}
    </Story>
</Preview>

### Funnel type Navigation Header

<Preview
    withSource='none'
>
    <Story
        name='Funnel type Header - Drupal'
        args={{
            elementPath : 'components/navigation-header/_doc/template.drupal',
            cartBtn,
            funnelNav,
            headerLogos,
            isFunnel : true
        }}
         argTypes={{
            rigthIconLinks  : {table: { disable: true }},
            hasTopBar       : {table: { disable: true }},
            hasAccountBtn   : {table: { disable: true }},
            hasNoSearchBtn  : {table: { disable: true }},
            hrefConnexion   : {table: { disable: true }},
            hrefLogOut      : {table: { disable: true }},
            isConnected     : {table: { disable: true }},
            labelConnexion  : {table: { disable: true }},
            labelLogOut     : {table: { disable: true }},
            navItems        : {table: { disable: true }},
            search_placeholder : {table: { disable: true }},
            topBar_items    : {table: { disable: true }},
            topBar_secondaryItems : {table: { disable: true }},
            accountHello    : {table: { disable: true }}
         }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>

## more info
