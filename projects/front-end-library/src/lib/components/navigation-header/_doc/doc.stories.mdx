import { moduleMetadata } from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs'

import { navItems, topBar_items, topBar_secondaryItems } from './data';
import { TwigContainerComponent }   from '../../../../utils/twig-container/twig-container.component';
import { HeaderComponent }          from '../angular/header.component';


<Meta 
    title='Components/Navigation Header' 
    component={HeaderComponent}
    decorators={[
        moduleMetadata({ declarations: [HeaderComponent, TwigContainerComponent]}),
        withDesign
    ]}
    parameters={{
        design: {
            type: 'figma',
            url: 'https://www.figma.com/file/7bTmk29aCBl7rCFszffYPr/DS-Library-UI-Kit-Pattern-lab?node-id=11017%3A708',
            disable: false
        },
    }}
    argTypes={{
        langcode: {
            defaultValue: "FR",
            control: { 
                type: "select",
                options: ["EN", "FR"],
            }
        },
        logo_name: {
            defaultValue: "videotron",
            control: {
                type: "select",
                options: ["videotron", "business"],
            }
        },
        hasTopBar: {
            defaultValue: true,
        },
        reversed: {
            defaultValue: false,
        }
    }}
/>


# Header

> Explore **[Design Documentation](https://zeroheight.com/79f1d3365/p/70ccb4--buttons)** (Zeroheight)

<br/>


## Drupal

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});

<Preview>
    <Story 
        name='Drupal'
        args={{ 
            elementPath : 'components/navigation-header/_doc/template.drupal',
            topBar_items,
            topBar_secondaryItems,
            navItems,
        }}
        height='500px'
    >
        {Drupal.bind({})}
    </Story>
</Preview>


## Source Code

```php
{% include '@bf-components/navigation-header/twig/index.twig' with {
    hasTopBar               : true,
    topBar                  : {
        items                   : [...],
        secondaryItems          : [...],
    },
    logo                    : {
        name                    : 'business',
    },
    navItems                : [...],
    hasMiniCartBtn          : false,
    hasNoSearchBtn          : false,
    reversed                : false,
} only %}
```

## Component API

<Props of={HeaderComponent} />


## Design Guidelines

<iframe width='100%' height='450' src='https://www.figma.com/file/7bTmk29aCBl7rCFszffYPr/DS-Library-UI-Kit-Pattern-lab?node-id=11017%3A708' frameBorder='0' allowFullScreen></iframe>