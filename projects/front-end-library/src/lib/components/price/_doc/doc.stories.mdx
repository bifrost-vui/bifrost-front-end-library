import { moduleMetadata }                       from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs/blocks';

import { PriceComponent }                       from '../angular/price.component';

import PriceTemplate                            from './template.drupal.twig';
import PriceTemplateNull                        from './template-null.drupal.twig';
import PriceTemplateAll                         from './template-all.drupal.twig';


<Meta 
    title='Components/Price' 
    component={PriceComponent}
    argTypes={{
        language: {
            control: { type: 'select' },
            table: { defaultValue: { summary: 'en' } },
        },
        size: {
            control: { type: 'select' },
            table: { defaultValue: { summary: 'small' } },
        },
        hasAsterisk: {
            table: { defaultValue: { summary: false } },
        },
        fullWidth: {
            table: { defaultValue: { summary: false } },
        },
        reversed: {
            table: { defaultValue: { summary: false } },
        },
    }}
/>


# Price

<br/>



## Component API

<Props of={PriceComponent} />



## Drupal implementation example

```jsx
{% include '@bf-components/price/twig/index.twig' with {
    upperTitle      : 'From',
    upfront         : {
        dollar          : 0
        label           : 'upfront',
    },
    dollar          : 30,
    dollarClass     : 'my-dollar-class',
    cent            : 99,
    hasAsterisk     : true,
    details         : '/month',
    promotion       : {
        priceStriked  : 40.99,
        priceSaved    : 10,
        savedLabel    : 'You saved:',
        superscript   : 1,
        direction     : 'horizontal',
    },
    message         : 'This is a message',
    class           : '',
    size            : 'medium',
    language        : 'en',
} only %}
```



## Drupal

export const commonProps = {
    upperTitle      : 'Upper Title',
    upfront         : {
        dollar          : 0,
        label           : 'upfront'
    },
    dollar          : 30,
    dollarClass     : '',
    cent            : 99,
    hasAsterisk     : false,
    details         : '/month',
    promotion       : {
        priceStriked  : 40.99,
        priceSaved    : 10,
        savedLabel    : 'You saved:',
        superscript   : 1,
        direction     : 'horizontal',
    },
    message         : 'This is a message.',
    class           : 'p-3',
}


export const Drupal = (args) => ({
    template: PriceTemplate(args),
    props: args,
});

<Canvas
    withSource='none'
>
    <Story 
        name='Drupal'
        args={{
            ...commonProps
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>

export const DrupalAll = (args) => ({
    template: PriceTemplateAll(args),
    props: args,
});

<Canvas
    withSource='none'
>
    <Story 
        name='Drupal - All Prices'
        args={{
            ...commonProps
        }}
    >
        {DrupalAll.bind({})}
    </Story>
</Canvas>


export const DrupalNull = (args) => ({
    template: PriceTemplateAll(args),
    props: args,
});

<Canvas
    withSource='none'
>
    <Story 
        name='Drupal - Null validator'
    >
        {DrupalNull.bind({})}
    </Story>
</Canvas>