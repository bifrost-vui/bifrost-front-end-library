import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs';

import { TabComponent }                         from '../angular/tab.component';
import TabTwigTemplate                          from './template.drupal.twig';


<Meta 
    title='Components/Tab' 
    component={TabComponent}
    argTypes={{
        reversed: {
            table: { defaultValue: { summary: false } },
        },
    }}
/>


# Tab

<br/>



## Component API

<Props of={TabComponent} />




## Drupal

export const Drupal = (args) => ({
    template: TabTwigTemplate(args),
    props: args,
});

<Canvas
    withSource='none'
>
    <Story 
        name='Drupal'
        args={{ 
            navItems        : [
                { label:'Channel selection',                href:'/channelSelection', class:'active' },
                { label:'Premium and speciality channels',  href:'/premiumChannels', class:'' },
                { label:'Remote control',                   href:'/remoteControl', class:'' },
                { label:'Home network',                     href:'/homeNetwork', class:'' }
            ]
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>

<Canvas
    withSource='none'
    style={{ height: '300px' }}
>
    <Story 
        name='Drupal - Single Tab'
        args={{
            navItems        : [
                { label:'Channel selection',                href:'/channelSelection', class:'active' },
            ]
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>
