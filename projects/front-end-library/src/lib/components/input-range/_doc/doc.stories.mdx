import { moduleMetadata }               from '@storybook/angular';
import { Meta, Story, Preview, Props }  from '@storybook/addon-docs/blocks';
import { withDesign }                   from 'storybook-addon-designs'
import { TwigContainerComponent }       from '../../../../utils/twig-container/twig-container.component';
import { InputRangeComponent }          from '../angular/input-range.component';

<Meta
    title='Components/Input Range'
    component={InputRangeComponent}
    decorators={[
        moduleMetadata({ declarations: [InputRangeComponent, TwigContainerComponent]}),
        withDesign
    ]}
    parameters={{
        design: {
            type    : 'figma',
            url     : 'https://www.figma.com/file/yFsS1z98l853oMi59vwbKI/D%C3%A9co-OC11---Design-UI?node-id=4%3A113',
            disable : false
        },
    }}
    argTypes={{
        minInputId : { type : { required: true }},
        maxInputId : { type : { required: true }},
        minInputValue : {
            type : {
                control : { type : 'number' },
                required : true
            }
        },
        maxInputValue : {
            type : {
                control : { type : 'number' },
                required : true
            }
        },
        step : {
            type : {
                control : { type : 'number' },
                required : true
            }
        },
        lang : {
            type : { defaultValue: 'en' },
            control : {
                type : 'select',
                options : ['en', 'fr']
            }
        },
        rangeHandles : {
            control : {
                type : 'select',
                options : ['','min', 'max']
            }
        }
    }}
/>

# Input Range



## Drupal

export const Drupal = (args) => ({
    component   : TwigContainerComponent,
    props       : args,
});

<Preview
    withSource='none'
    style={{ height: '400px' }}
>
    <Story
        name='Drupal'
        args={{
            elementPath : 'components/input-range/_doc/template.drupal',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>


## Drupal implementation exemple

### Default Input range component with 2 handles

```jsx
{% include '@bf-components/input-range/twig/index.twig' with {
    minInputId     : 'minRangeId',
    maxInputId     : 'maxRangeId',
    minInputValue  : 20,
    maxInputValue  : 800,
    minStartValue  : 40,
    maxStartValue  : 160,
    labelUnit      : '$',
    step           : 1,
    lang           : 'en',
    minLegend      : 'Min legend line 1',
    maxLegend      : 'Max legend line 1',
    minLegendDescription  : 'Min legend line 2',
    maxLegendDescription  : 'Max legend line 2',
    class          : ''
} only %}
```
### Input range component with min handle only

```jsx
{% include '@bf-components/input-range/twig/index.twig' with {
    minInputId     : 'minRangeId',
    maxInputId     : 'maxRangeID',
    minInputValue  : 20,
    maxInputValue  : 800,
    minStartValue  : 40,
    labelUnit      : '$',
    step           : 1,
    lang           : 'en',
    rangeHandles   : 'min'
    minLegend      : 'Min legend line 1',
    maxLegend      : 'Max legend line 1',
    minLegendDescription  : 'Min legend line 2',
    maxLegendDescription  : 'Max legend line 2',
    class          : ''
} only %}
```
### Input range component with max handle only

```jsx
{% include '@bf-components/input-range/twig/index.twig' with {
    minInputId     : 'minRangeId',
    maxInputId     : 'maxRangeID',
    minInputValue  : 20,
    maxInputValue  : 800,
    maxStartValue  : 160,
    labelUnit      : '$',
    step           : 1,
    lang           : 'en',
    rangeHandles   : 'max'
    minLegend      : 'Min legend line 1',
    maxLegend      : 'Max legend line 1',
    minLegendDescription  : 'Min legend line 2',
    maxLegendDescription  : 'Max legend line 2',
    class          : ''
} only %}
```


## Component API

**✅ Inputs are equivalent for Angular and Drupal.**

<Props of={InputRangeComponent} />


## Design Guidelines

<iframe width='100%' height='450' src='https://www.figma.com/embed?embed_host=share&url=https%3A%2F%2Fwww.figma.com%2Ffile%2FyFsS1z98l853oMi59vwbKI%2FD%25C3%25A9co-OC11---Design-UI%3Fnode-id%3D3410%253A214676' frameBorder='0' allowFullScreen></iframe>
