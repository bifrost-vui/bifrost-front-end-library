import { moduleMetadata }               from '@storybook/angular';
import { Meta, Story, Preview, Props }  from '@storybook/addon-docs/blocks';
import { withDesign }                   from 'storybook-addon-designs'
import { TwigContainerComponent }       from '../../../../utils/twig-container/twig-container.component';
import { InputRangeComponent }          from '../angular/input-range.component';

<Meta
    title='Components/Input Range'
    component={InputRangeComponent}
    decorators={[
        moduleMetadata({ declarations: [InputRangeComponent, TwigContainerComponent]}),
        withDesign
    ]}
    parameters={{
        design: {
            type    : 'figma',
            url     : 'https://www.figma.com/file/yFsS1z98l853oMi59vwbKI/D%C3%A9co-OC11---Design-UI?node-id=4%3A113',
            disable : false
        },
    }}
    argTypes={{
        step     : {
            type : { defaultValue: 1 },
        },
        language : {
            type : { defaultValue: 'en' },
            control : {
                type : 'select',
            }
        },
    }}
/>

# Input Range

## Drupal

export const defaultProps = {
    min             : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max             : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit       : '$',
    language        : '',
}
export const startProps = {
    initialValue    : [40,80],
    min             : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max             : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit       : '$',
    language        : '',
}

export const minProps = {
    initialValue    : 80,
    min             : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max             : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit       : '$',
    language        : '',
}

export const maxProps = {
    initialValue    : 40,
    min             : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max             : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit       : '$',
    isInverted      : true,
    language        : '',
}



export const Drupal = (args) => ({
    component   : TwigContainerComponent,
    props       : args,
});

<Preview
    withSource='none'
    style={{ height: '400px' }}
>
    <Story
        name='Drupal - 2 handles'
        args={{
            elementPath : 'components/input-range/_doc/template.drupal',
            ...defaultProps
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>

<Preview
    withSource='none'
    style={{ height: '400px' }}
>
    <Story
        name='Drupal - 2 handles with initial values'
        args={{
            elementPath : 'components/input-range/_doc/template.drupal',
            ...startProps
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>

<Preview
    withSource='none'
    style={{ height: '400px' }}
>
    <Story
        name='Drupal - 1 handle'
        args={{
            elementPath : 'components/input-range/_doc/template.drupal',
            ...minProps
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>

<Preview
    withSource='none'
    style={{ height: '400px' }}
>
    <Story
        name='Drupal - 1 handle inverted direction'
        args={{
            elementPath : 'components/input-range/_doc/template.drupal',
            ...maxProps
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>


## Drupal implementation exemple


### Default Input range component with 2 handles with range

```jsx
{% include '@bf-components/input-range/twig/index.twig' with {
    initialValue   : [40,80]|40|null
    min            : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max            : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit      : '$',
    inverted    : true|false
    language    : '',
    class       : ''

} only %}
```
### Input range component with min handle only

```jsx
{% include '@bf-components/input-range/twig/index.twig' with {
    min            : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max            : {
        inputId       : 'maxRange',
        value         : 150,
        start         : 80,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit      : '$',
    step           : 5,
    rangeHandles   : 'min',
    language       : '',
    class          : ''
} %}
```
### Input range component with max handle only

```jsx
{% include '@bf-components/input-range/twig/index.twig' with {
    min            : {
        inputId       : 'minRange',
        value         : 20,
        start         : 40,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max            : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit      : '$',
    step           : 10,
    rangeHandles   : 'max',
    language       : '',
    class          : ''
} %}
```


## Component API

**✅ Inputs are equivalent for Angular and Drupal.**

<Props of={InputRangeComponent} />

### More
```
range        : [40,80]| 40 | null
isInverted   : true|false
```
- if initialValue is empty or undefined : creates a 2 handles slider positioned at each end of the input track
- if initialValue contains 2 values : creates a 2 handles slider positioned at the corresponding coordonates
- if initialValue contains 1 value : creates a 1 handle slider positioned  at the corresponding coordonate
- if isInverted is set true and range contains 1 value : reverts the direction of the 1 handle slider but has no effect if initialValue contains 2 values


## Design Guidelines

<iframe width='100%' height='450' src='https://www.figma.com/embed?embed_host=share&url=https%3A%2F%2Fwww.figma.com%2Ffile%2FyFsS1z98l853oMi59vwbKI%2FD%25C3%25A9co-OC11---Design-UI%3Fnode-id%3D3410%253A214676' frameBorder='0' allowFullScreen></iframe>
