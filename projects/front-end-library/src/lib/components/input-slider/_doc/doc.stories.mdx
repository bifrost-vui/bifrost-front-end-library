import { moduleMetadata }               from '@storybook/angular';
import { Meta, Story, Preview, Props }  from '@storybook/addon-docs/blocks';
import { withDesign }                   from 'storybook-addon-designs'
import { TwigContainerComponent }       from '../../../../utils/twig-container/twig-container.component';
import { InputSliderComponent }          from '../angular/input-slider.component';

<Meta
    title='Components/Input Slider'
    component={InputSliderComponent}
    decorators={[
        moduleMetadata({ declarations: [InputSliderComponent, TwigContainerComponent]}),
        withDesign
    ]}
    parameters={{
        design: {
            type    : 'figma',
            url     : 'https://www.figma.com/file/yFsS1z98l853oMi59vwbKI/D%C3%A9co-OC11---Design-UI?node-id=4%3A113',
            disable : false
        },
    }}
    argTypes={{
        step     : {
            defaultValue: 1,
            table: {defaultValue: { summary: '1' }},
        },
        unitPosition : {
            defaultValue: 'end',
            table: {defaultValue: { summary: 'end' }},
            control : {
                type : 'select',
            }
        },
    }}
/>

# Input Slider

## Component API

**✅ Inputs are equivalent for Angular and Drupal.**

<Props of={InputSliderComponent} />

### Notes
- if `value` is empty or undefined : creates a 2 handles slider positioned at each end of the input track
- if `value` contains 2 values : creates a 2 handles slider positioned at the corresponding coordonates
- if `value` contains 1 value : creates a 1 handle slider positioned  at the corresponding coordonate
- if `isInverted` is set true and `value` contains 1 value : reverts the direction of the 1 handle slider but has no effect if `value` contains 2 values


## Drupal implementation exemple


export const defaultProps = {
    min             : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max             : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit       : '$',
}
export const startProps = {
    value    : [40,80],
    min             : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max             : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit       : '$',
}

export const minProps = {
    value    : 80,
    min             : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max             : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit       : '$',
}

export const maxProps = {
    value    : 40,
    min             : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max             : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit       : '$',
    isInverted      : true,
}



export const Drupal = (args) => ({
    component   : TwigContainerComponent,
    props       : args,
});


### Default Input slider component with 2 handles
<Preview
    withSource='none'
    style={{ height: '400px' }}
>
    <Story
        name='Drupal - 2 handles'
        args={{
            elementPath : 'components/input-slider/_doc/template.drupal',
            ...defaultProps
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>

```jsx
{% include '@bf-components/input-slider/twig/index.twig' with {
    min            : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max            : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit      : '$',
    language       : '',
    class          : ''

} only %}
```
### Default Input slider component with 2 handles with initial values
<Preview
    withSource='none'
    style={{ height: '400px' }}
>
    <Story
        name='Drupal - 2 handles with initial values'
        args={{
            elementPath : 'components/input-slider/_doc/template.drupal',
            ...startProps
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>


```jsx
{% include '@bf-components/input-slider/twig/index.twig' with {
    value   : [40,80]
    min            : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max            : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit      : '$',
    unitPosition   : 'before',

} only %}
```
### Input slider component with min handle only

<Preview
    withSource='none'
    style={{ height: '400px' }}
>
    <Story
        name='Drupal - 1 handle'
        args={{
            elementPath : 'components/input-slider/_doc/template.drupal',
            ...minProps
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>

```jsx
{% include '@bf-components/input-slider/twig/index.twig' with {
    value    : 40,
    min             : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max             : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit       : '$',
    isInverted      : true,
} %}
```
### Input slider component with max handle only

<Preview
    withSource='none'
    style={{ height: '400px' }}
>
    <Story
        name='Drupal - 1 handle inverted direction'
        args={{
            elementPath : 'components/input-slider/_doc/template.drupal',
            ...maxProps
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>

```jsx
{% include '@bf-components/input-slider/twig/index.twig' with {
    value    : 40,
    min             : {
        inputId       : 'minRange',
        value         : 20,
        legend        : 'Minimum',
        description   : '$20/month'
    },
    max             : {
        inputId       : 'maxRange',
        value         : 150,
        legend        : 'Maximum',
        description   : '$150/month'
    },
    labelUnit       : '$',
    isInverted      : true,
    class           : ''
} %}
```

## Design Guidelines

<iframe width='100%' height='450' src='https://www.figma.com/embed?embed_host=share&url=https%3A%2F%2Fwww.figma.com%2Ffile%2FyFsS1z98l853oMi59vwbKI%2FD%25C3%25A9co-OC11---Design-UI%3Fnode-id%3D3410%253A214676' frameBorder='0' allowFullScreen></iframe>
