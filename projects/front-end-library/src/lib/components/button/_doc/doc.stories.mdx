import { moduleMetadata }                       from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs';

import { interfaceIcons }                       from '../../icon/_doc/data.ts';
import { ButtonComponent }                      from '../angular/button.component';

import ButtonTwigTemplate                       from './template.drupal.twig';
import ButtonTwigTemplateSpecials               from './template-specials.drupal.twig';
import ButtonTwig                               from '../twig/index.twig';



<Meta 
    title='Components/Button' 
    component={ButtonComponent}
    argTypes={{
        hierarchy: {
            control: {
                type: 'select',
                options: ['', 'primary', 'primary-alt', 'secondary', 'tertiary' ]
            }
        },
        iconName: {
            control: {
                type: 'select',
                options: ['', 'placeholder', ...interfaceIcons.map(a => a.list).flat().sort()]
            }
        },
        iconPosition: {
            control: {
                type: 'select',
                options: ['', 'left', 'right']
            }
        },
    }}
/>


# Button

<br/>



## Component API

<Props of={ButtonComponent} />



## Drupal implementation example

```jsx
{% include '@bf-components/button/twig/index.twig' with {
    label           : 'Button label',
    hierarchy       : 'primary',
    isDisabled      : false,
    isLoading       : false,
    iconName        : 'placeholder',
    iconPosition    : 'right',
    class           : 'mt-3',
} only %}
```


## Drupal

export const Drupal = (args) => ({
    template: ButtonTwigTemplate(args),
    props: args,
});

<Canvas
    withSource='none'
>
    <Story 
        name='Drupal'
        args={{ 
            class           : 'ml-4 mb-3',
            dataDismiss     : 'dataDismiss',
            dataToggle      : 'dataToggle',
            dataTarget      : 'dataTarget',
            ariaControls    : 'ariaControls',
            ariaExpanded    : 'ariaExpanded',
            ariaLabel       : 'ariaLabel',
            extraAttribute  : 'extraAttribute',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>


export const DrupalSpecials = (args) => ({
    template: ButtonTwigTemplateSpecials(args),
    props: args,
});

<Canvas
    withSource='none'
>
    <Story 
        name='Drupal - Specials'
        args={{
            class       : 'ml-4 mb-3',
        }}
    >
        {DrupalSpecials.bind({})}
    </Story>
</Canvas>