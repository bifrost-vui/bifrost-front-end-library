import { moduleMetadata }                       from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs/blocks';

import { interfaceIcons }                       from '../../icon/_doc/data.ts';
import { TwigContainerComponent }               from '../../../../utils/twig-container/twig-container.component';
import { ButtonComponent }                      from '../angular/button.component';
import ButtonTwigTemplate                       from './template.drupal.twig';
import ButtonTwigTemplateCSSClassOnly           from './template-css-class-only.drupal.twig';
import ButtonTwigTemplateAppStore               from './template-app-store.twig';



<Meta 
    title='Components/Button' 
    component={ButtonComponent}
    decorators={[
        moduleMetadata({ declarations: [ButtonComponent, TwigContainerComponent]})
    ]}
    argTypes={{
        hierarchy: {
            control: {
                type: 'select',
                options: ['', 'primary', 'primary-alt', 'secondary', 'tertiary' ]
            }
        },
        iconName: {
            control: {
                type: 'select',
                options: ['', 'placeholder', ...interfaceIcons.map(a => a.list).flat().sort()]
            }
        },
        iconPosition: {
            control: {
                type: 'select',
                options: ['', 'left', 'right']
            }
        },
    }}
/>


# Button

<br/>



## Component API

<Props of={ButtonComponent} />



## Drupal implementation example

```jsx
{% include '@bf-components/button/twig/index.twig' with {
    label           : 'Button label',
    hierarchy       : 'primary',
    isDisabled      : false,
    isLoading       : false,
    iconName        : 'placeholder',
    iconPosition    : 'right',
    class           : 'mt-3',
} only %}
```


## Drupal

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});

<Preview
    withSource='none'
    style={{ height: '700px' }}
>
    <Story 
        name='Drupal'
        args={{ 
            elementPath     : 'components/button/_doc/template.drupal',
            class           : 'ml-4 mb-3',
            dataDismiss     : 'dataDismiss',
            dataToggle      : 'dataToggle',
            dataTarget      : 'dataTarget',
            ariaControls    : 'ariaControls',
            ariaExpanded    : 'ariaExpanded',
            ariaLabel       : 'ariaLabel',
            extraAttribute  : 'extraAttribute',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>

## Buttons: CSS Class Only

export const DrupalSpecials = (args) => ({
    template: ButtonTwigTemplateCSSClassOnly(args),
    props: args,
});

<Preview
    withSource='none'
    style={{ height: '700px' }}
>
    <Story 
        name='Drupal - CSS class only'
        args={{
            class       : 'ml-4 mb-3',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>



## Buttons: App Store and Google Play

export const DrupalAppStore = (args) => ({
    template: ButtonTwigTemplateAppStore(args),
    props: args,
});

<Canvas
    withSource='none'
>
    <Story 
        name='Drupal - AppStore and Google Play'
    >
        {DrupalAppStore.bind({})}
    </Story>
</Canvas>
</Canvas>
