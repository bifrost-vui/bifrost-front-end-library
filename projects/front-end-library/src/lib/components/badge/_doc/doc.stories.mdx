import { moduleMetadata }                   from '@storybook/angular';
import { ArgsTable, Canvas, Meta, Story }   from '@storybook/addon-docs';
import illustrationIcons                    from '../../../../../../../public/icons/list/illustrations.json';
import { IconComponent }                    from '../../icon/angular/icon.component';
import { BadgeComponent }                   from '../angular/badge.component';
import { BadgeTemplateComponent }           from './template.angular.component';
import TwigTemplate                         from './template.drupal.twig';


<Meta
    title='Components/Badge'
    component={BadgeComponent}
    decorators={[
        moduleMetadata({ declarations: [BadgeComponent, IconComponent]})
    ]}
    argTypes={{
        hierarchy: {
            table:   { defaultValue: { summary: 'secondary' } },
            control: { type: 'select' }
        },
        angle: {
            control: { type: 'select' }
        },
        noUppercase: {
            table: { defaultValue: { summary: false } },
        },
        iconName: {
            defaultValue: '',
            control: {
                type: 'select',
                options: ['', 'placeholder', ...illustrationIcons.map(a => a.list).flat().sort()]
            }
        },
        _disabled: {
            table:{ disable: true }
        }
    }}
/>

# Badge
<br/>

## Component API
<ArgsTable of={BadgeComponent} />

## Drupal implementation example

```jsx
{% include '@bf-components/badge/twig/index.twig' with {
    label: 'Free',
    class: 'my-2',
    hierarchy: 'primary',
    iconName: 'camera'
} only %}
```

## Drupal

export const Drupal = (args) => ({
    template: TwigTemplate(args),
    props: args,
});

<Canvas
    withSource='none'
    style={{ height: '600px' }}
>
    <Story
        name='Drupal'
        args={{
            class: 'my-2',
            reversed: undefined,
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>

<Canvas
    withSource='none'
    style={{ height: '600px' }}
>
    <Story
        name='Drupal - Multiline'
        args={{
            class: 'my-2',
            reversed: undefined,
            label: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. '
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>

<Canvas
    withSource='none'
    style={{ height: '600px'}}
>
    <Story
        name='Drupal - Disabled'
        args={{
            class: 'my-2',
            reversed: undefined,
            _disabled: true
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>


## Angular

export const Angular = (args) => ({
    component: BadgeTemplateComponent,
    props: args,
});

<Canvas
    withSource='none'
    style={{ height: '600px' }}
>
    <Story
        name='Angular'
        args={{
            className: 'my-2',
            reversed: undefined,
        }}
    >
        {Angular.bind({})}
    </Story>
</Canvas>

<Canvas
    withSource='none'
    style={{ height: '600px' }}
>
    <Story
        name='Angular - Multiline'
        args={{
            className: 'my-2',
            reversed: undefined,
            label: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. '
        }}
    >
        {Angular.bind({})}
    </Story>
</Canvas>

<Canvas
    withSource='none'
    style={{ height: '600px' }}
>
    <Story
        name='Angular - Disabled'
        args={{
            className: 'my-2',
            reversed: undefined,
            _disabled: true
        }}
    >
        {Angular.bind({})}
    </Story>
</Canvas>