{% set reversed = reversed|json_parse|default(false) %}
{% set activeStep = activeStep|json_parse %}
{% set navItemSteps = navItemSteps|json_parse %}
{% set hasStepCounter = hasStepCounter|json_parse|default(false) %}
{% set counterLabel = counterLabel|json_parse %}
{% set previousHasLabel = previousHasLabel|json_parse|default(true) %}
{% set nextHasLabel = nextHasLabel|json_parse|default(true) %}
{% set nextIsClickable = nextIsClickable|json_parse|default(false) %}

<div class='{% if reversed %}reversed bf-color-bg-ground{% endif %}'>
    {% include '@bf-components/stepper/twig/index.twig' %}
</div>