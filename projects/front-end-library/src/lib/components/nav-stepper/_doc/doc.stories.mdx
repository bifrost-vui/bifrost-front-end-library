import { moduleMetadata }               from '@storybook/angular';
import { Meta, Story, Preview, Props }  from '@storybook/addon-docs/blocks';
import { withDesign }                   from 'storybook-addon-designs';

import { TwigContainerComponent }       from '../../../../utils/twig-container/twig-container.component';
import { NavStepperComponent }          from '../angular/nav-stepper.component';





<Meta
    title='Components/Navigation Stepper'
    component={NavStepperComponent}
    decorators={[
        moduleMetadata({ declarations: [NavStepperComponent, TwigContainerComponent]}),
        withDesign
    ]}
    parameters={{
        design: {
            type    : 'figma',
            url     : 'https://www.figma.com/file/7bTmk29aCBl7rCFszffYPr/DS---Components-%26-styles?node-id=14042%3A3427',
            disable : false
        },
    }}
    argTypes={{
        activeStep: {
            defaultValue : 1,
            type:  { control : { type: 'number' },
            }
        },
        previousHasLabel: {
            defaultValue : true
        },
        nextHasLabel: {
            defaultValue : true
        }
    }}
/>

# Stepper

 This component enables the end user to visualise his progression when he's going trough a multi-step process.
 It's used with a `Funnel` type navigation header.
 It's navigation capacities are limited to the previous step and next step within the current step by step process

## Drupal
export const commonProps = {
    navItemSteps : [
        {label:"first step",href:"activeStep:1"},
        {label:"Channel Selection", href:"activeStep:2"},
        {label:"Order Review", href:"activeStep:3"},
        {label:"checkout", href:"activeStep:4"},
        {label:"Step 5", href:"activeStep:5"},
        {label:"Step 6", href:"activeStep:6"},
        {label:"Step 7", href:"activeStep:7"},
        {label:"Step 8", href:"activeStep:8"},
        {label:"last step", href:"activeStep:9"}
    ]
}

export const Drupal = (args) => ({
    component   : TwigContainerComponent,
    props       : args,
});

<Preview
    withSource='none'
    style={{ height: '300px' }}
>
    <Story
        name='Drupal'
        args={{
            elementPath : 'components/nav-stepper/_doc/template.drupal',
            ...commonProps,
        }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>


## Drupal implementation exemple

```jsx
{% include '@bf-components/nav-stepper/twig/index.twig' with {
    activeStep : 1,
    navItemSteps : [
        {label:"Craft Bundle", href:"#craftBundle", class:"active"},
        {label:"Channel Selection", href:"#chanelSelection"},
        {label:"Order Review", href:"#OrderReview"},
        {label:"Checkout", href:"#checkout"}
    ]
} only %}
```



## Angular implementation exemple

Please, read the [Implementation Page](/?path=/docs/implementation-angular--page).



## Component API

**✅ Inputs are equivalent for Angular and Drupal.**

<Props of={NavStepperComponent} />


## Design Guidelines

Check the [Figma](https://www.figma.com/file/qpI1TDuVG9ciRWsoOIQ93x/Navigation?node-id=939%3A1615) for more details

https://www.figma.com/file/7bTmk29aCBl7rCFszffYPr/DS---Components-%26-styles?node-id=14042%3A3427
