{% include "@bf-components/card/twig/index.twig" with {
  badge: badge|default(null),
  buttons: buttons|default(null),
  class: "bf-card--phone " ~ (class|default(null)),
  contentBadges: contentBadges|default(null),
  contentLists: contentLists|default(null),
  description_html: description_html|default(null),
  detailsColors: detailsColors|default(null),
  detailsStorage: detailsStorage|default(null),
  image: image|default(null),
  isDisabled: isDisabled ?? false,
  language: language|default("en"),
  link: link|default(null),
  message: message|default(null),
  messageLink: messageLink|default(null),
  note: note|default(null),
  price: price|default(null),
  promotionBadges: promotionBadges|default(null),
  subtitle: subtitle|default(null),
  title: title|default(null),
  titleTag: titleTag|default(null),
  upperTitle: upperTitle|default(null),
  visualBackgroundColor: visualBackgroundColor|default(null),
} only %}
