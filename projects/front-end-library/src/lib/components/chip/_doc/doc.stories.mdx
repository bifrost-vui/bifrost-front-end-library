import { moduleMetadata }               from '@storybook/angular';
import { Canvas, Meta, Story, Props }   from '@storybook/addon-docs';
import { withDesign }                   from 'storybook-addon-designs'
import { TwigContainerComponent }       from '../../../../utils/twig-container/twig-container.component';
import { ChipComponent }                from '../angular/chip.component';

<Meta
    title='Components/Chip'
    component={ChipComponent}
    decorators={[
        moduleMetadata({ declarations: [ChipComponent, TwigContainerComponent]}),
        withDesign
    ]}
    parameters={{
        design: {
            type    : 'figma',
            url     : 'https://www.figma.com/file/7bTmk29aCBl7rCFszffYPr/DS---Components-%26-styles?node-id=10083%3A667',
            disable : false
        },
    }}
    argTypes={{
        label   : {type : { required: true } }
    }}
/>

# Chip

## Drupal

export const Drupal = (args) => ({
    component   : TwigContainerComponent,
    props       : args,
});

<Canvas
    withSource='none'
    style={{height : '300px'}}
>
    <Story
        name='Drupal'
        args={{
            elementPath : 'components/chip/_doc/template.drupal',
            label       : 'Chip',
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>


## Drupal implementation exemple

```jsx
{% include '@bf-components/chip/twig/index.twig' with {
    label : 'Chip',
} only %}
```

## Component API

**✅ Inputs are equivalent for Angular and Drupal.**

<Props of={ChipComponent} />


## Design Guidelines

<iframe width='100%' height='450' src='https://www.figma.com/embed?embed_host=share&url=https%3A%2F%2Fwww.figma.com%2Ffile%2F7bTmk29aCBl7rCFszffYPr%2FDS---Components-%2526-styles%3Fnode-id%3D10083%253A667' frameBorder='0' allowFullScreen></iframe>
