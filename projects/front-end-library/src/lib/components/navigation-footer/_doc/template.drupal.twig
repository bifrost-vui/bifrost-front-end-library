
{# ----------------------------------------------------------------- #}
{% set links                = links|json_parse %}
{% set linkFields           = linkFields|json_parse %}
{% set hasBackToTopLink     = hasBackToTopLink|json_parse|default(true) %}
{% set isReduced            = isReduced|json_parse|default(false) %}
{% set reversed             = reversed|json_parse|default(false) %}


{# ----------------------------------------------------------------- #}
{% include '@bf-components/navigation-footer/twig/index.twig' with {
    links               : links,
    linkFields          : linkFields,
    logo                : {
        name                : logo_name|default(''),
        href                : logo_href|default('#'),
    },
    hasBackToTopLink    : hasBackToTopLink,
    languageBlock       : languageBlock,
    isReduced           : isReduced,
    reversed            : reversed,
    langcode            : langcode|default('EN'),
} only %}
