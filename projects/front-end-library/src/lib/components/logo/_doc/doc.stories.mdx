import { moduleMetadata } from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs'

import { TwigContainerComponent }   from '../../../../utils/twig-container/twig-container.component';
import { LogoComponent }          from '../angular/logo.component';


<Meta 
    title='Components/Logo' 
    component={LogoComponent}
    decorators={[
        moduleMetadata({ declarations: [LogoComponent, TwigContainerComponent]}),
        withDesign
    ]}
    parameters={{
        design: {
            type: 'figma',
            url: 'https://www.figma.com/file/7bTmk29aCBl7rCFszffYPr/DS-Library-UI-Kit-Pattern-lab?node-id=11017%3A708',
            disable: false
        },
    }}
    argTypes={{
        name: {
            defaultValue: "videotron",
            control: { 
                type: "select",
                options: ["videotron", "business"],
            }
        },
        langcode: {
            defaultValue: "FR",
            control: { 
                type: "select",
                options: ["EN", "FR"],
            }
        },
        reversed: {
            defaultValue: true,
        },
    }}
/>


# Logo

> Explore **[Design Documentation](https://zeroheight.com/79f1d3365/p/70ccb4--buttons)** (Zeroheight)

<br/>

## Drupal

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});

<Preview>
    <Story 
        name='Drupal'
        args={{ 
            elementPath : 'components/logo/_doc/template.drupal',
        }}
        height='500px'
    >
        {Drupal.bind({})}
    </Story>
</Preview>


## Source Code

```php
{% include '@bf-components/logo/twig/index.twig' with {
    langcode    : 'EN',
    name        : 'business',
} only %}
```


## Component API

<Props of={LogoComponent} />