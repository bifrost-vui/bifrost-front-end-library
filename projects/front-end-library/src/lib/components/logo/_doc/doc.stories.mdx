import { moduleMetadata } from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import { allLogoCategories }                from './data';
import { TwigContainerComponent }           from '../../../../utils/twig-container/twig-container.component';
import { LogoComponent }                    from '../angular/logo.component';


<Meta 
    title='Components/Logo' 
    component={LogoComponent}
    decorators={[
        moduleMetadata({ declarations: [LogoComponent, TwigContainerComponent]}),
    ]}
    argTypes={{
        name: {
            defaultValue: 'videotron',
            control: { 
                type: 'select',
                options: ['videotron', 'business'],
            }
        },
        size: {
            defaultValue: 'medium',
            control: { 
                type: 'select',
                options: ['large', 'medium', 'small'],
            }
        },
        langcode: {
            defaultValue: 'FR',
            control: { 
                type: 'select',
                options: ['EN', 'FR'],
            }
        },
        allLogoCategories: {
            control: { disable: true }
        }
    }}
/>


# Logo

<br/>



## Component API

<Props of={LogoComponent} />




## Drupal implementation example

```jsx
{% include '@bf-components/logo/twig/index.twig' with {
    langcode    : 'EN',
    name        : 'business'
} only %}
```



## Drupal

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args,
});

<Preview
    withSource='none'
    style={{ height: '300px' }}
>
    <Story 
        name='Drupal'
        args={{ 
            elementPath : 'components/logo/_doc/template.drupal',
        }}
        height='160px'
    >
        {Drupal.bind({})}
    </Story>
</Preview>



## Drupal All Logos

<Preview
    withSource='none'
    style={{ height: '600px' }}
>
    <Story 
        name='Drupal - All logos'
        args={{ 
            elementPath : 'components/logo/_doc/template-all.drupal',
            allLogoCategories
        }}
        height='160px'
    >
        {Drupal.bind()}
    </Story>
</Preview>