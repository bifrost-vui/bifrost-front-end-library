import { Canvas, Meta, Story, Preview, Props } from '@storybook/addon-docs';

import { allLogoCategories }                from './data';
import { LogoComponent }                    from '../angular/logo.component';

import LogoTemplateTwigAll                  from './template-all.drupal.twig';
import LogoTemplateTwig                     from './template.drupal.twig';


<Meta 
    title='Components/Logo' 
    component={LogoComponent}
    argTypes={{
        name: {
            control: { type: 'select' },
            table: { defaultValue: { summary: 'videotron' } },
        },
        size: {
            control: { type: 'select' },
            table: { defaultValue: { summary: 'medium' } },
        },
        langcode: {
            control: { 
                type: 'select'
            },
            table: { defaultValue: { summary: 'en' } },
        },
        reversed: {
            table: { defaultValue: { summary: false } },
        },
        allLogoCategories: {
            table: { disable: true }
        }
    }}
/>


# Logo

<br/>



## Component API

<Props of={LogoComponent} />




## Drupal implementation example

```jsx
{% include '@bf-components/logo/twig/index.twig' with {
    name        : 'business',
    size        : 'large',
    langcode    : 'en',
} only %}
```



## Drupal

export const Drupal = (args) => ({
    template: LogoTemplateTwig(args),
    props: args,
});

<Canvas
    withSource='none'
>
    <Story 
        name='Drupal'
    >
        {Drupal.bind({})}
    </Story>
</Canvas>



## Drupal All Logos

<Canvas
    withSource='none'
>
    <Story 
        name='Drupal - All logos'
        args={{ 
            allLogoCategories
        }}
    >
        {Drupal.bind()}
    </Story>
</Canvas>