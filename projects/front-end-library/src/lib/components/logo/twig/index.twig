{% set name     = (name is defined and name is same as ('business')) ? 'videotron-business': name %}
{% set fileBaseName = name|default('videotron')%}
{% set size     = size|default('medium') %}
{% set langcode = langcode|default('fr') %}


<div class='bf-logo d-inline-flex align-items-center justify-content-center {{ class }}'>
{# <!-- {{ 'logo-' ~ fileBaseName|lower ~ '-' ~ size|lower ~ '-' ~ langcode|lower ~ '.svg' }} --> #}
    {% include '@bf-components/logo/svg/logo-' ~ fileBaseName|lower ~ '-' ~ size|lower ~ '-' ~ langcode|lower ~ '.svg' ignore missing %}
</div>
