import { moduleMetadata }                       from '@storybook/angular';
import { Canvas, Meta, Story, Preview, Props }  from '@storybook/addon-docs';
import { withDesign }                           from 'storybook-addon-designs'
import { TwigContainerComponent }               from '../../../../utils/twig-container/twig-container.component';
import { InputRadioComponent }                  from '../angular/input-radio.component';

<Meta
    title='Components/Input Radio'
    component={InputRadioComponent}
    decorators={[
        moduleMetadata({ declarations: [InputRadioComponent, TwigContainerComponent]}),
        withDesign
    ]}
    parameters={{
        design: {
            type    : 'figma',
            url     : 'https://www.figma.com/file/7bTmk29aCBl7rCFszffYPr/DS---Components-%26-styles?node-id=16890%3A229796',
            disable : false
        },
    }}
    argTypes={{
        id: {
            type: { required : true }
        },
        inputName: {
            control: { type: 'text' },
            description: 'UI-Kit legacy - Deprecated. Use `name` instead'
        },
        inputId: {
            control: { type: 'text' },
            description: 'UI-Kit legacy - Deprecated. Use `id` instead'
        },
        labelTxtClass : {
            control: { type: 'text' },
            description: 'UI-Kit legacy - Deprecated. Use `labelClass` instead'
        },
        _background :{
            control : {
                type : 'select',
                options : [null, 'bf-color-bg-ground','bf-color-bg-underground','reversed bf-color-bg-ground','reversed bf-color-bg-underground', 'bf-color-bg-highlight']
            },
            description : 'preview the component on selected background'
        }
    }}
/>

# Input Radio


## Drupal

export const Drupal = (args) => ({
    component   : TwigContainerComponent,
    props       : args,
});

## Component API

**✅ Inputs are equivalent for Angular and Drupal.**

<Props of={InputRadioComponent} />

<Canvas
    withSource='none'
    style={{ height: '400px' }}
>
    <Story
        name='Drupal'
        args={{
            elementPath : 'components/input-radio/_doc/template.drupal',
            id          : '',
            inputId     : 'inputId',
            name        : 'name',
            label       : 'label'
        }}
    >
        {Drupal.bind({})}
    </Story>
</Canvas>


## Drupal implementation exemple

```jsx
{% include '@bf-components/input-radio/twig/index.twig' with {
    id      : 'id',
    value   : 'value',
    name    : 'name',
    label   : 'label'
} only %}
```


## Design Guidelines

<iframe width='100%' height='450' src='https://www.figma.com/embed?embed_host=share&url=https%3A%2F%2Fwww.figma.com%2Ffile%2F7bTmk29aCBl7rCFszffYPr%2FDS---Components-%2526-styles%3Fnode-id%3D16890%253A229796' frameBorder='0' allowFullScreen></iframe>