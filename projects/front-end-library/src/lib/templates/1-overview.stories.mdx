import { Story, Meta, Preview, Props } from '@storybook/addon-docs/blocks';
import { linkTo } from '@storybook/addon-links';
import { moduleMetadata } from '@storybook/angular';
import { TileComponent } from '../../utils/tile/tile.component';
import { Stack, Tile } from '../../utils/documentation-style.js';

<Meta
    title='Templates/All Templates'
    parameters={{ 
        layout: 'padded', 
        viewMode: 'docs',
        previewTabs: { canvas: { hidden: true } }
    }}
    decorators={[
        moduleMetadata({ declarations: [TileComponent]})
    ]}
/>

# All Templates

<br/>

Templates are used for references only. In the futur, it should be the responsability of the application.

<br/>

<Stack>

<Tile onClick={linkTo('templates-contact-form--default-story')} status='pending'>
    Contact Form
    
    <iframe title="Default" src="iframe.html?id=templates-contact-form--default-story&amp;viewMode=story"></iframe>
</Tile>

</Stack>