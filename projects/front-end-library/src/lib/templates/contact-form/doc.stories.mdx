import { moduleMetadata } from '@storybook/angular';
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { TwigContainerComponent } from '../../../utils/twig-container/twig-container.component';
import { ContactFormComponent } from './angular/contact-form.component';

<Meta 
    title="Templates/Contact Form" 
    component={ContactFormComponent} 
    parameters={{
        layout: 'padded',
    }}
    decorators={[
        moduleMetadata({ declarations: [ContactFormComponent, TwigContainerComponent]})
    ]}
/>

# Template Contact Form

Lorem ipsum dolor sit amet.

export const ContactFormStory = (args) => ({
  component: ContactFormComponent,
  props: args,
});

<Preview>
  <Story name="Default" args={{ label: 'Label' }}>
    {ContactFormStory.bind({})}
  </Story>
</Preview>

## Drupal

export const Drupal = (args) => ({
    component: TwigContainerComponent,
    props: args
});

<Preview>
    <Story 
        name="Drupal"
        args={{ 
            elementPath: 'templates/contact-form/twig/contact-form',
            label: 'Text Drupal',
        }}
        parameters={{ docs: { iframeHeight: 150 } }}
    >
        {Drupal.bind({})}
    </Story>
</Preview>

## Component API
**✅  Inputs are equivalent for Angular and Drupal.**

<Props of={ContactFormComponent} />